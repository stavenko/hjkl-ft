# Bugs

## Open

### `weekly-recompute-skips-in-silence` — недельный пересчёт отказывается молча — ПРИЧИНА НАЙДЕНА 2026-08-05
- **Где:** `maybe_recompute_weekly_planka` / `maybe_recompute_weekly_steps_planka` (`frontend/src/services/letters.rs`).
- **Чем оказался конкретный случай:** НЕ поломкой расчёта. 2026-07-31 куратор записал пользователю планку через директиву `set_planka`; `set_calorie_goal` → `mark_planka_recomputed()` поставил якорь в эту дату. На 2026-08-05 прошло 5 дней из 7, пересчёт законно не запускался — он наступает 2026-08-07. Директива куратора перезапускает недельные часы, о чём говорит и её собственное письмо.
- **Что остаётся багом:** ни один из трёх выходов не оставлял следа, поэтому пять дней разбирательства ушли на то, что видно из одной строки. Выходы: планки ещё нет; с якоря прошло меньше семи дней; срок вышел, но за семь завершённых дней нет ни одной записи в дневнике (третий к тому же НЕ двигает якорь и повторяется бесконечно).
- **Сделано:** каждый выход называет причину в консоли, а два, где срок УЖЕ вышел, пишут её в журнал ошибок приложения, видимый на экране.
- **Открытый вопрос (решение за пользователем):** должна ли ручная планка куратора сдвигать недельный цикл. Сейчас сдвигает.

### `calcium-values-unreliable` — кальций: разброс до 5× и устойчивые промахи
- **Где:** `lookup_nutrient` для «Кальций» (`frontend/src/services/ai.rs`).
- **Замер (dev, 2026-08-05):** десять продуктов с выдуманными брендами, по десять запросов на каждый — 100 запросов тем же промптом и той же схемой, что в приложении. Ошибок разбора: **0 из 100**.

  | продукт | справ., мг | медиана | min–max | разброс |
  |---|---|---|---|---|
  | Вкустаун Творог обезжиренный 0,5% | 120 | 112.5 | 100–135 | 1.4× |
  | Вкустаун Йогурт натуральный 3,2% | 120 | 125 | 100–450 | 4.5× |
  | Вкустаун Молоко 2,5% | 120 | 100 | 100–120 | 1.2× |
  | Сырный Дом Пармезан 40% | 1180 | 1000 | 1000–1000 | 1× |
  | Сырный Дом Моцарелла 45% | 505 | 237.5 | 150–800 | 5.3× |
  | СояПлюс Тофу классический | ~350 | 187.5 | 75–350 | 4.7× |
  | Зелёный Луг Кунжут неочищенный | 975 | 220 | 210–220 | 1× |
  | Зелёный Луг Шпинат свежий | 99 | 120 | 120–120 | 1× |
  | Зелёный Луг Рис белый шлифованный | 10 | 20 | 7–20 | 2.9× |
  | Доброфлот Сайра натуральная | не установлено | 225 | 150–225 | 1.5× |

- **Что хорошо:** тракт стабилен — ни одного сбоя разбора на сотне запросов. Жидкие молочные, творог, пармезан и шпинат ложатся в ±20 % от справочных.
- **Что плохо — два разных дефекта:**
  1. **Разброс.** Моцарелла 150–800, тофу 75–350, йогурт 100–450. Записывается `recommended` ОДНОГО вызова, то есть какое из этих чисел попадёт в базу — лотерея. У тофу широкий разброс отчасти честный (реальные 100–680 в зависимости от коагулянта), у моцареллы и йогурта — нет.
  2. **Устойчивый промах.** Кунжут: 220 мг при справочных 975, разброс 1× — модель отвечает одинаково и одинаково неверно. Похоже, отвечает про очищенный кунжут (~60–150). Повторы такое не лечат: это не шум, а заблуждение.
- **Не установлено:** справочное значение для консервированной сайры. Модельные 225 мг я подтвердить не могу — сравнивать не с чем.
- **Замерочный скрипт:** `scratchpad/calcium-consistency.sh` (промпт и схема скопированы из `lookup_nutrient` дословно, иначе мерилось бы не то, что работает).

### `sse-answer-truncated` — ответ модели обрывается на середине — НЕ ВОСПРОИЗВОДИТСЯ
- **Где:** тракт ai-worker → arti-pipes → `generate_validated`.
- **Что было записано:** сетевой ответ приходит оборванным посреди JSON — `…"recommended":15` (49 символов), `…"comment":"Г` (64 символа).
- **Проверка (dev, 2026-08-05):** восемь прямых запросов через `curl` — восемь целых ответов, `finish_reason=stop`, маркер `[DONE]` на месте, 49–87 чанков. Обрыва нет ни в одном.
- **Вывод:** те «обрывы» я читал через `response.text()` в Playwright на ПОТОКОВОМ ответе — так тело SSE читать нельзя, оно отдаётся частями. Дефект приписан тракту ошибочно. Запись оставлена: если обрыв всплывёт в журнале приложения (а не в моём перехвате), здесь уже описано, чем он НЕ является.

## Fixed

### `iron-week-has-no-deadline` — у недельной планки железа не назван срок — FIXED 2026-08-05
- **Где:** подпись под индикаторами (`frontend/src/components/progress_widget.rs`).
- **Симптом:** гейты шагов и кальция говорят «Держите индикатор … зелёным 7 дней» и «Осталось N дней»; у железа подписи не было вовсе — срок недельной планки нигде не назывался.
- **Фикс:** ветка `iron_gate_title` в той же цепочке гейтов: «Выполните планку по железу за эту неделю.» + «Осталось N дней», где N — остаток недели железа. Подпись исчезает, как только недельная норма набрана.

### `nutrients-two-storage-shapes` — одно поле хранилось в двух формах — FIXED 2026-08-05
- **Где:** `db::put_untracked` (`frontend/src/services/db.rs`).
- **Симптом:** `Food.nutrients` (`BTreeMap`) при локальной записи ложился в IndexedDB как JS `Map` (дефолтный `serde_wasm_bindgen`), а строка, приехавшая синком, — обычным объектом (`Serializer::json_compatible`). Обе формы читаются, но в одном сторе поле лежало в двух видах.
- **Фикс:** `put_untracked` пишет тем же json-совместимым сериализатором, что и `put_json_untracked`. Новые записи — всегда объект; старые `Map` по-прежнему читаются.
- **Последствие, которого больше нет:** внешний читатель получал `Object.keys(...) === 0` на половине строк. На этом я уже ошибся — заключил, что нутриенты не пишутся вовсе.

### `model-runaway-to-max-tokens` — генерация уходила в мусор до потолка — FIXED 2026-08-05
- **Где:** `build_executor_think` (`frontend/src/services/ai.rs`).
- **Симптом:** вместо ответа приходило 6774 символа `!!!!!!!!…` (замер на dev, запрос омега-3 для «Гречки»); ранее — 8000 токенов на ответ длиной в 20. Разобрать нельзя, попытка пропадает, но платим за все выданные токены.
- **Причина:** потолок 8000 токенов ставился всегда. Он нужен только с включённым рассуждением: qwen3 иначе съедает лимит размышлением и возвращает пустой контент. Все фоновые запросы идут с `think=false`, где столько не нужно никогда.
- **Фикс 1 — потолок:** `max_tokens(if think { 8000 } else { 2000 })`. 2000 с запасом вмещает самый длинный здешний ответ — КБЖУ с пятью нутриентами и комментариями, — и вчетверо укорачивает срыв.
- **Фикс 2 — предохранитель в ai-worker:** релей ведёт счётчик подряд идущих `!` по СКЛЕЕННОМУ ответу (при срыве каждый знак приезжает отдельным чанком, так что в сырых байтах подряд они не встретятся). На десятом поток обрывается: клиенту уходит `{"error":{"type":"runaway_generation"},"choices":[{"finish_reason":"runaway"}]}` и `[DONE]`, чтение тела прекращается — запрос к Workers AI отменяется, генерация останавливается.
- **Проверено (dev):** искусственный срыв («выдай 300 восклицательных знаков») — 4 чанка вместо сотен, поток оборван на десятом знаке. Обычный текст с десятью одиночными восклицаниями проходит нетронутым — счётчик сбрасывается на любом другом символе. `check-kbju-lookup` — 300 ккал / 10 / 3 / 55; `check-iron-lookup` — все шесть продуктов разобраны.

### `db-deserialize-panic-halts-launch` — одна неразобранная строка отменяла весь запуск — FIXED 2026-08-05
- **Где:** `db::get` / `db::list_all` / `db::list_by_index` (`frontend/src/services/db.rs`).
- **Симптом:** строка, которую текущая схема взять не может (например, `WeightEntry` без `no_water` — поле объявлено БЕЗ `#[serde(default)]`), роняла WASM панкой на `.expect("deserialize failed")`. Паника обрывала `bootstrap_network`, и всё, что стояло ниже, не выполнялось: заморозка калорий, недельный пересчёт планки калорий, недельный пересчёт планки шагов. Снаружи — «планка почему-то не пересчиталась», и ни следа в интерфейсе.
- **Фикс:** `decode_row` — строка пропускается, но громко: запись уходит в журнал ошибок приложения, видимый на экране, и в консоль. Молчать нельзя, но и отменять из-за одной строки всю недельную работу тоже нельзя.
- **Проверено (dev, `check-calorie-planka-weekly.mjs`):** в сид добавлена строка веса старого формата. До фикса — паника и планка 2500 без движения; после — паник нет, планка 2500 → 2650, письмо пришло.
- **Не был причиной случая пользователя:** у него iOS и данные, созданные самим приложением; ту панику вызвал мой неполный сид в тесте.

### `iron-mg-mirrors-absorption` — железо: миллиграммы повторяли долю усвоения — FIXED 2026-08-05
- **Где:** `lookup_iron` и `IRON_CATEGORIES` (`frontend/src/services/ai.rs`).
- **Симптом:** содержание железа приходило равным доле усвоения того же продукта — печень 0.25 мг вместо ~9, чечевица 0.05 вместо ~7.5, говядина 0.2 вместо ~2.6. Все значения лежали в 0…0.25, ровно в диапазоне таблицы усвоения.
- **Причина:** доли усвоения в таблице были ЕДИНСТВЕННЫМИ числами в промпте, и модель списывала их в поле миллиграммов. Проверка `min ≤ recommended ≤ max` такие ответы пропускала: внутри блока они согласованы.
- **Фикс:** долю усвоения у модели больше не спрашивают — её даёт `IRON_CATEGORIES` по выбранной строке (валидатор и так связывал её таблицей, модель без таблицы даёт чечевице те же 0.20, что говядине). В таблице промпта вместо долей стоит диапазон СОДЕРЖАНИЯ в мг, и он же обязывает: значение вне диапазона строки — отказ и повтор. Единственные числа в промпте теперь относятся ровно к тому полю, куда их надо класть.
- **Проверено (dev, `check-iron-lookup.mjs`):** печень 0.25 → **10** мг, говядина 0.2 → **2.5**, чечевица 0.05 → **5**, творог 0.02 → **0.2**. Усвоение строго из таблицы: dairy 0.02, liver_offal 0.25, meat_red 0.2, legumes 0.05.
- **Осталось неточным:** яблоку модель даёт 0.5 мг при справочных ~0.12 — это уже разброс в пределах строки, а не системная ошибка.

### `schema-echoed-as-answer` — модель отвечала схемой вместо значения — FIXED 2026-08-05
- **Где:** `SingleNutrient` (`frontend/src/services/ai.rs`).
- **Симптом:** вместо значения приходил документ JSON Schema на 1099 символов, начинавшийся с `{"description":"A focused, FLAT single-nutrient answer. Deliberately not the nested \`Nutr…` — то есть с доккоммента к нашей же структуре.
- **Причина:** `schemars` превращает `///` в `description` схемы, а ai-worker (`lib.rs:381`) вклеивает схему в системное сообщение текстом со словами «The JSON MUST conform to this exact schema». Доккоммент был рассказом про наш код, и модель зачитывала его как задание. `response_format` при этом до Workers AI не доезжает — в `run_params` его нет, никакого structured output не существует, схема работает только как уговор в промпте.
- **Фикс:** развёрнутые пояснения переведены из `///` в `//` у `SingleNutrient`, `FoodVerdicts`, `MatchResult`. Заодно снят `unwrap_schema_envelope` — костыль, вычленявший ответ из схемы-эха.
- **Проверено:** прогон `check-food-sweep` на dev, 10 запросов, эха нет ни в одном ответе.

### Bottom nav visible on the PWA install onboarding screen — FIXED 2026-06-21
- **Where:** `AppState::PwaPrompt` overlay (`frontend/src/app.rs`).
- **Symptom:** the fixed bottom nav (z-index:40) showed through at the bottom of the "Food Tracker — install the app" onboarding screen, because `PwaPrompt` (a normal-flow `min-height:100vh` div) created no stacking context above it — unlike the `Auth`/`PushOnboarding` overlays.
- **Fix:** wrapped the `PwaPrompt` overlay in `position:fixed; inset:0; z-index:100; background:var(--bulma-scheme-main); overflow-y:auto`, matching the other overlays.
- **Verified (Playwright):** with the prompt showing, `document.elementFromPoint` at the nav's center returns the overlay `DIV`, not the `A[data-testid=nav-story]` link, and the nav is not visible in a screenshot.
