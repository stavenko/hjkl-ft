# Типизированная запись дневника: готовая и ленивая

**Статус:** ЗАПИСЬ ДИКТОВКИ, часть 1. Требования зафиксированы со слов заказчика,
диктовка продолжится. Код НЕ пишем: ни решений по схеме хранения, ни по миграции,
ни по UI здесь ещё нет — только то, что сказано, плюс карта затрагиваемых мест в
текущем коде и вопросы, которые диктовка пока не закрыла.

---

## 1. Что требуется

Запись еды в дневнике становится **типизированной и универсальной**. Сегодня
запись дневника — всегда одно и то же: ссылка на продукт плюс граммы
(`DiaryEntry`, `common/api-types/src/lib.rs:307`). Вместо этого запись — **одно из
двух**:

### 1.1. Готовая запись

- ссылка на готовый продукт (как сейчас) + количество в граммах;
- создаётся из поиска по имеющимся продуктам.

### 1.2. Ленивая запись

Содержит то, что человек дал «как есть», не разбирая на продукты:

- **описание** того, что он съел;
- **набор изображений** — их может быть несколько;
- изображение может быть **этикеткой** или **фотографией еды**.

У ленивой записи **два состояния**:

**Состояние A — создана, но не распознана.**
Запись уже лежит в дневнике, человек её видит. Окончательную калорийность
посчитать пока нельзя. Распознавание идёт **в фоне, по тому же принципу, что и
прочие нутриенты** (существующий фоновый проход — `frontend/src/services/classify.rs`
и `enrich.rs`). По завершении запись переходит в состояние B.

**Состояние B — распознана.**
Запись может содержать **ссылки на несколько продуктов**: по сути это объединение
нескольких `FoodRecord` — ссылка на `FoodItem` плюс количество еды.

---

## 2. Как это ложится на текущий код

Разбор текущего процесса записи еды — в `docs/food-photo-recognition.md` (§3) и в
файлах ниже. Здесь только те места, которых требование касается напрямую.

| Что | Где сейчас | Чего требование касается |
|---|---|---|
| Тип записи дневника | `common/api-types/src/lib.rs:307` (`DiaryEntry`) | становится типизированным: готовая / ленивая |
| Запись из поиска | `food_picker.rs:20`, `diary_add.rs:78` → `local::save_food_to_diary` (`local.rs:792`) | это и есть «готовая запись», путь сохраняется |
| Запись по описанию | вкладка 0 `food_editor.rs:965`, `ai::lookup` (`ai.rs:255`) | описание перестаёт быть предусловием записи: сначала запись, потом распознавание |
| Запись по этикетке | вкладка 1 `food_editor.rs:1019`, `ai::vision_direct` / очередь (`ai.rs:2967`+) | изображение-этикетка становится содержимым ленивой записи |
| Запись по фото еды | вкладка 2 `food_editor.rs:596-827`, `ai::food_items_*` (`ai.rs:3310`, `:3355`) | сегодня распознаётся СИНХРОННО перед записью; должно уходить в фон |
| Список продуктов из одного фото | `local::add_detected_foods_to_diary` (`local.rs:1259`) — по отдельному `Food` + отдельной записи на позицию (решение D6) | заменяется на ОДНУ запись, объединяющую несколько ссылок на продукт |
| Фоновый проход | `classify::enqueue` (`classify.rs`), `enrich.rs`, `flags_pipeline.rs`, `iron_pipeline.rs` | тот самый «принцип, по которому распознаются прочие нутриенты» |
| Драфты | `FoodDraft` (`api-types:326`), `local::save_draft` (`local.rs:1081`) | пересекается с «ленивой записью»; отношение двух сущностей диктовкой не закрыто |
| Хранение и синк | `db.rs:110` (`ALL_STORES`), `sync.rs:100`, `:284`, `:445`, `:649` | новые поля/сторы придётся провести через версию БД и списки синка |

Отдельно: сегодняшнее решение **D6** из `docs/food-photo-recognition.md` («bulk-add
создаёт отдельный `Food` + отдельную diary-запись на каждую позицию») прямо
противоречит состоянию B — там одна запись объединяет несколько ссылок. Требование
его отменяет; документ по фото надо будет поправить, когда схема устоится.

---

## 3. Чего диктовка пока не сказала

Не блокирует продолжение — просто помечено, чтобы не додумывать молча:

1. **Имена сущностей.** Сказано «`FoodRecord` — ссылка на `FoodItem` с количеством».
   В коде сегодня `Food` и `DiaryEntry`; переименование это или новые типы поверх —
   не сказано.
2. **Что показывать в дневнике у нераспознанной записи.** Ноль калорий, прочерк,
   оценка? На дневные шкалы и планки (`indicators.rs`) она влиять обязана как-то.
3. **Провал распознавания.** Фоновый проход у нас умеет сдаваться после трёх
   попыток (`classify.rs:37-42`). Что тогда с записью — остаётся ленивой навсегда,
   зовёт человека руками, удаляется?
4. **Правка после распознавания.** Сегодня список позиций правится ДО записи в
   дневник (`food_editor.rs:1214-1294`). Теперь запись уже в дневнике — где и когда
   человек правит граммы и убирает inferred-позиции (масло/майонез).
5. **Хранение изображений.** Сегодня фото живут в памяти формы как base64 и в
   дневник не попадают. Ленивая запись их хранит — значит, вопрос размера,
   IndexedDB и синка (фото прогресса лежат в `local.rs:2214` — возможный образец).
6. **Судьба драфтов** (`food_drafts`) при появлении ленивой записи.
7. **Обратная совместимость** уже записанных дневников.

---

## 4. Дальше

Продолжение диктовки — сюда же, в §1. Как только требования будут полны,
отдельным заходом: схема данных, миграция, план работ.
