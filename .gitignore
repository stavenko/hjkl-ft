/target
/dist
/data
data.db
data.db-shm
data.db-wal
node_modules
frontend/styles/output.css
frontend/dist
.playwright-mcp/
screenshot-*.png
/*.png
.wrangler/
*.tgz
test-results/
e2e/test-results/

.env
renorma-lp.jpeg
# Abandoned standalone pay app prototype — kept local, superseded by the Telegram Mini App.
/pay/
.telegram-webhook-secret
.lava-hook-api-key
.admin-approve-secret
.lava-api-key

# Логи прогона регрессии (scripts/run-regression.mjs).
regression-*.log

# Настройка Playwright под конкретную машину прогона (браузер из образа, прокси,
# ограничение версии TLS). У каждого своя — в репозиторий не идёт.
/e2e/pw-local.config.ts

# Следы прогона Playwright: снимки, трассы и отметка о последнем запуске.
# Пишутся каждым прогоном и относятся к машине, а не к проекту.
/e2e/test-results/
# Снимки экранов из `e2e/shot-*.mjs` — делаются на ходу и живут в переписке.
/e2e/*.png
